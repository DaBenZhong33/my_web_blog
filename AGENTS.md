# AGENTS.md

## 语言和编码

- 除代码、命令、路径、标识符和必要的英文原文外，所有面向用户的提问和回答都使用中文。
- 本地系统是 Windows。
- 代码库统一使用 UTF-8 编码。
- PowerShell 中查看中文字符时可能出现乱码；读取、编辑或生成包含中文的文件时，必须显式关注编码。
- 修改包含中文文本的代码后，必须检查代码中是否存在乱码，并在需要时修复。

## 项目概况

- 这是一个 Vue 3 + Vite 项目。
- 入口文件在 `src/main.js`，路由在 `src/router.js`，主要页面位于 `src/views/`。
- 全局样式位于 `src/style.css`，项目数据目前位于 `src/data/projects.js`。

## 常用命令

- 安装依赖：`npm install`
- 本地开发：`npm run dev`
- 生产构建：`npm run build`
- 本地预览构建产物：`npm run preview`

## 开发约定

- 优先遵循仓库中已有的 Vue、CSS 和数据组织方式，避免引入不必要的新抽象。
- 搜索文件或文本时优先使用 `rg` 或 `rg --files`。
- 修改前先确认相关文件的现状；不要覆盖用户未要求修改的内容。
- 不要执行 `git reset --hard`、`git checkout --` 等破坏性命令，除非用户明确要求。
- 完成修改前，按影响范围运行必要的验证命令；至少确认被修改文件能以 UTF-8 正常读取。
