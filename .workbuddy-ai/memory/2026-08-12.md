# 2026-08-12

## 新增滚动交互（4 项）

- 新建 `src/composables/useScrollProgress.js`：全局滚动进度/scrollY，rAF 节流，供组件复用。
- 新建 `src/components/ScrollProgress.vue`：视口顶部 2px accent 进度条，挂在 App.vue，全站生效。
- 新建 `src/components/BackToTop.vue`：右下角圆形按钮，SVG 圆环 stroke-dashoffset 绑滚动进度，滚过 0.8 屏后出现，点击平滑回顶（尊重 prefers-reduced-motion）。
- `src/style.css`：自定义滚动条（::-webkit-scrollbar 窄条 + hover accent，Firefox scrollbar-width/color）。
- `src/views/HomeView.vue`：
  - 跑马灯（signal-track）用 WAAPI playbackRate 联动滚动速度：下滚加速、上滚可反转、停滚衰减回 1 倍速。
  - work 作品区改为纵向驱动横向滚动：`.work-hscroll`（JS 设高度 = 100vh + 轨道超出宽度）+ `.work-sticky` + `.work-track` translate3d；≤1080px 回退为纵向网格。

## 环境备忘

- 沙箱 safe-delete 会拦截并导致 `vite build` 清空 dist 失败（trash operation aborted）。验证构建可用 `npx vite build --emptyOutDir false`。
